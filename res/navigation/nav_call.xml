<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_call"
    app:startDestination="@id/fragmentFriend">
    <fragment
        android:id="@+id/fragmentFriend"
        android:name="com.example.mysterymessage.ui.AddFriend.FragmentFriend"
        android:label="Friend" >
        <action
            android:id="@+id/action_fragmentFriend_to_fragmentSecretMessage"
            app:destination="@id/fragmentSecretMessage" />
    </fragment>
    <fragment
        android:id="@+id/fragmentSecretMessage"
        android:name="com.example.mysterymessage.ui.MessageOptionDialog.SecretMessage.FragmentSecretMessage"
        android:label="FragmentSecretMessage" />
</navigation>